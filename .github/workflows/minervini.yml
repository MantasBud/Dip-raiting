name: Minervini palyginimas

on:
  workflow_dispatch:
    inputs:
      metai:
        description: "Kiek metu istorijos"
        required: false
        default: "10"

jobs:
  minervini:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install yfinance pandas numpy tzdata
      - name: Patikrinti failus
        run: |
          test -f backtest_minervini.py || (echo "KLAIDA - failas nerastas" && exit 1)
          test -f universas.py || (echo "KLAIDA - universas.py nerastas" && exit 1)
      - name: Europos universas
        env:
          M: ${{ github.event.inputs.metai || '10' }}
        run: python backtest_minervini.py --rinka eu --metai "$M" | tee minervini_eu.txt
      - name: JAV universas
        if: always()
        env:
          M: ${{ github.event.inputs.metai || '10' }}
        run: python backtest_minervini.py --rinka us --metai "$M" | tee minervini_us.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: minervini-rezultatai
          path: |
            minervini_eu.txt
            minervini_us.txt
          if-no-files-found: ignore
